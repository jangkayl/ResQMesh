# Legacy documentation snapshot

This directory preserves the pre-migration ResQMesh documentation, `CONTEXT.md`, and `GEMINI.md` as they existed when the active Codex context was consolidated on 2026-09-16.

The material includes historical audits, speculative architecture, Antigravity context, obsolete prompts, session narratives, and earlier current-state documents. It is preserved for recovery and historical review, but it is not implementation guidance and is excluded from normal Codex routing.

Use the root `AGENTS.md` and the canonical files under `docs/` for current work. Read an archived file only when the user explicitly asks for historical context or when investigating the origin of a specific decision.
